#### centos 安装monit
1. `yum install monit`
2. 编辑配置文件`/etc/monitrc` 修改配置
3. 常用命令
    
   + `monit -t`测试配置文件是否正确  
   + `monit summary`查看摘要信息  
   + `monit status`查看服务运行信息  

#### ubuntu 安装monit
1. `apt-get install monit`
